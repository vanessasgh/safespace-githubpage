function getLocationsHandler() {
    // TODO: trigger on submission of map's view "search location component"
    let keyword = "";
    getLocationsHandler({ keyword: keyword })
    // TODO: get values from field and pass them as args, then pushes it to the map view
}