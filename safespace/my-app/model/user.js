function User(email, username, password, university) {
    // TODO: Define User object, takes in the attribute values as given in args and return the newly created object
}

function getUserInfoByID(id) {
    // TODO: Return an User object that match the given id in argument. If none match, return empty.
    return User()
}

function createUser(email, username, password, university) {
    // TODO: Create a new User object in the data store with attribute values as given in args and return the newly created object. If args are not valid for an User, throw an error with appropriate message.
    return User()
}

function updateUserInfo(email, username, password, university) {
    // TODO: Updates an existing User object in the data store with attribute values as given in args and return the updated object. If args are not valid for an User, throw an error with appropriate message.
    return User()
}